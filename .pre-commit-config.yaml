repos:
  - repo: local
    hooks:
      - id: check-secrets-format
        name: Check secrets format (COINBASE keys)
        entry: python scripts/check_secrets_format.py
        language: system
        files: 
          - ".streamlit/secrets.toml"
        stages: [commit]
